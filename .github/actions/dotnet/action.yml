name: Set up .NET
description: Sets up .NET SDK for battler-bug-reporter.

runs:
  using: "composite"
  steps:
    - name: Set up .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: "10.0.x"
